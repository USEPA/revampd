name: Testing Cron

on:
  schedule: 
    - cron: "*/5 * * * 3"

jobs:
  push: 
    name: Push master to Bitbucket
    runs-on: "ubuntu-latest"
    steps: 
      - name: Push to Bitbucket
        run: |
            echo "running"

